<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/pago1"
    tools:context=".cliente.ConfirmarPagoActivity">

    <!-- Nombre del Restaurante como Header -->
    <TextView
        android:id="@+id/nombre_restaurante"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Paga a"
        android:textSize="25sp"
        android:textStyle="bold"
        android:textColor="#090909"
        android:gravity="center"

        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    <!-- Línea horizontal debajo del header -->

    <!-- Monto a Pagar en el Centro -->

    <!-- Botón Aceptar Pago al Final -->
    <TextView
        android:id="@+id/monto_pagar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Monto a pagar"
        android:textColor="#101010"
        android:textStyle="bold"

        android:textSize="35sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.403" />

    <Button
        android:id="@+id/btn_aceptar_pago"
        android:layout_width="280dp"
        android:layout_height="50dp"
        android:layout_margin="32dp"
        android:backgroundTint="#F5F1F6"
        android:text="Yapear"
        android:textColor="#070707"
        android:textStyle="bold"
        android:textSize="19sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
